name: CI

on:
  push:
    branches:
      - main
      - 'feature/*'
  pull_request:
    branches:
      - main

jobs:
  lint_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Install pnpm and Biome globally
        run: npm install -g pnpm @biomejs/biome

      - name: Run Biome Checks (Lint & Format)
        run: biome check ./

      - name: Run tests
        run: pnpm test